def main = fn(() => int(
  def numbers = list((int) => [1, 2, 3])
  def number = int(numbers[1]) ; base 0 so local-int = 2
  printf("%ld\n", number)

  def functions = list((fn() => int) => [
    fn(() => int(0)),
    fn(() => int(1)),
    fn(() => int(2)),
  ])

  0
))
