#include "runtime/expressions.h"

rt_value_t *eval_expression(expression_t *expr, scope_t *scope) {
  // Evaluate expression
  for (size_t i = 0; i < expr->factors->cur_size; ++i) {
  }

  return NULL;
}
